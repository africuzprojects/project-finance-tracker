# Local overrides only — never commit real keys
.env
.env.*
!.env.example

# Optional local env injection (if you copy instead of using Vercel)
js/env.local.js

node_modules/
www/

# Capacitor native projects (optional: remove android/ from ignore if you want to commit them)
# android/
.vercel
